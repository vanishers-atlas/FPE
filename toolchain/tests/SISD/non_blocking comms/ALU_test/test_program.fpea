{
  ADD(1, 1, ACC);
  MOV(ACC, PUT[0]);
  ADD(1, ACC, ACC);
  MOV(ACC, PUT[0]);

  AND(3, 5, PUT[0]);

  MUL(GET[0]<ADV>, GET[1]<ADV>, ACC);
  MOV(ACC, PUT[0]);

  MUL(GET[0]<ADV>, GET[1]<ADV>, ACC);
  MOV(ACC, PUT[0]);

  MUL(GET[0]<ADV>, GET[1]<ADV>, ACC);
  MOV(ACC, PUT[0]);
}
